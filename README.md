# RAG-Ассистент

Интеллектуальный ассистент, который отвечает на вопросы пользователя, используя информацию из базы знаний.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка](#установка)
- [Настройка](#настройка)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Компоненты системы](#компоненты-системы)
- [Оценка качества](#оценка-качества)
- [Технологии](#технологии)
- [Лицензия](#лицензия)

## 🎯 Описание

RAG-ассистент — это система вопросов и ответов, которая объединяет возможности векторного поиска и генерации текста с помощью больших языковых моделей. Система автоматически находит релевантные фрагменты документов в базе знаний и использует их для формирования точных и контекстуальных ответов.

### Основные преимущества:

- ✅ **Точность**: Ответы основаны на реальных документах из базы знаний
- ✅ **Кеширование**: Быстрые ответы на повторяющиеся вопросы
- ✅ **Гибкость**: Поддержка двух LLM провайдеров (GigaChat и OpenAI)
- ✅ **Оценка качества**: Встроенная система оценки через RAGAS метрики

## ✨ Возможности

- 🔍 **Семантический поиск** по базе документов с использованием векторных представлений
- 💬 **Интеллектуальные ответы** на основе найденного контекста
- 💾 **Кеширование** результатов для оптимизации производительности
- 📊 **Статистика** использования системы и кеша
- 🎯 **Оценка качества** ответов через метрики RAGAS (Faithfulness, Context Precision)
- 🔄 **Два режима работы**: GigaChat и OpenAI API

## 🏗️ Архитектура

Система состоит из следующих основных компонентов:

```
┌─────────────┐
│   Пользователь   │
└──────┬──────┘
       │ Запрос
       ▼
┌─────────────────┐
│   RAG Pipeline  │
└──────┬──────────┘
       │
       ├──► Кеш (SQLite) ──┐
       │                    │
       │                    ▼
       │              Ответ из кеша
       │
       └──► Векторное хранилище (ChromaDB)
                  │
                  ▼
            Поиск релевантных документов
                  │
                  ▼
            Формирование промпта с контекстом
                  │
                  ▼
            Генерация ответа (LLM)
                  │
                  ▼
            Сохранение в кеш
```

## 📦 Требования

- Python 3.11 или выше
- API ключ от одного из провайдеров:
  - **GigaChat**: `GIGACHAT_AUTH_KEY` и `GIGACHAT_RQUID`
  - **OpenAI**: `OPENAI_API_KEY`

## 🚀 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd rag-assistant
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**
   - Windows (PowerShell):
     ```powershell
     .\venv\Scripts\Activate.ps1
     ```
   - Windows (CMD):
     ```cmd
     venv\Scripts\activate.bat
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

## ⚙️ Настройка

1. **Создайте файл `.env` в корне проекта:**

Для режима GigaChat:
```env
GIGACHAT_AUTH_KEY=your-auth-key-here
GIGACHAT_RQUID=your-rquid-here
```

Для режима OpenAI:
```env
OPENAI_API_KEY=your-openai-api-key-here
```

2. **Подготовьте данные:**

Поместите ваши документы в текстовый файл:
- `assistant_giga/data/docs.txt` — для режима GigaChat
- `assistant_api/data/docs.txt` — для режима OpenAI

Формат файла: обычный текстовый файл в кодировке UTF-8. Документы могут быть разделены пустыми строками или специальными маркерами.

## 💻 Использование

### Режим GigaChat

Запустите консольное приложение:

```bash
cd assistant_giga
python app.py
```

После запуска вы увидите приветственный баннер. Введите свой вопрос и нажмите Enter.

**Доступные команды:**
- `exit` или `quit` — выход из приложения
- `stats` — просмотр статистики системы
- `clear` — очистка кеша

**Пример использования:**
```
💭 Ваш вопрос: Что такое машинное обучение?

────────────────────────────────────────────────────────────
📝 Вопрос: Что такое машинное обучение?
────────────────────────────────────────────────────────────
🤖 Источник: GigaChat
   Использовано документов: 3

💬 Ответ:
Машинное обучение — это раздел искусственного интеллекта...
```

### Режим OpenAI API

Для использования API режима, перейдите в директорию `assistant_api` и используйте модули напрямую:

```python
from rag_pipeline import RAGPipeline

pipeline = RAGPipeline(
    collection_name="api_rag_collection",
    cache_db_path="api_rag_cache.db",
    data_file="data/docs.txt",
    model="gpt-4o-mini"
)

result = pipeline.query("Что такое RAG?")
print(result['answer'])
```

### Оценка качества (RAGAS)

Для оценки качества системы в режиме OpenAI API:

```bash
cd assistant_api
python evaluate_ragas.py
```

Скрипт выполнит оценку по метрикам:
- **Faithfulness** — точность ответа относительно контекста
- **Context Precision** — качество извлечённого контекста

## 📁 Структура проекта

```
rag-assistant/
│
├── assistant_giga/          # Реализация с GigaChat
│   ├── app.py               # Консольное приложение
│   ├── rag_pipeline.py      # Основной RAG pipeline
│   ├── gigachat_client.py   # Клиент для GigaChat API
│   ├── vector_store.py      # Векторное хранилище
│   ├── cache.py             # Модуль кеширования
│   ├── data/
│   │   └── docs.txt         # База знаний
│   └── chroma_db/           # Данные ChromaDB
│
├── assistant_api/           # Реализация с OpenAI API
│   ├── rag_pipeline.py      # Основной RAG pipeline
│   ├── vector_store.py      # Векторное хранилище
│   ├── evaluate_ragas.py    # Оценка качества через RAGAS
│   ├── data/
│   │   └── docs.txt         # База знаний
│   └── chroma_db/           # Данные ChromaDB
│
├── requirements.txt         # Зависимости проекта
├── .env                     # Переменные окружения (создать самостоятельно)
└── README.md                # Документация
```

## 🔧 Компоненты системы

### 1. VectorStore (Векторное хранилище)

Отвечает за:
- Загрузку и обработку документов
- Разбиение текста на семантические чанки
- Создание векторных представлений (embeddings)
- Поиск релевантных документов по запросу

**Особенности:**
- Умное разбиение текста с учётом структуры (абзацы, предложения)
- Перекрытие между чанками для сохранения контекста
- Использование ChromaDB для хранения векторов

### 2. RAGCache (Кеш)

Отвечает за:
- Хранение пар вопрос-ответ
- Быстрый доступ к ранее заданным вопросам
- Статистику использования кеша

**Особенности:**
- Использование SQLite для хранения
- Нормализация запросов для лучшего совпадения
- Хранение контекста вместе с ответами

### 3. RAGPipeline (Основной pipeline)

Отвечает за:
- Координацию работы всех компонентов
- Обработку запросов пользователя
- Формирование промптов с контекстом
- Генерацию ответов через LLM

**Процесс обработки запроса:**
1. Проверка кеша
2. Поиск релевантных документов
3. Формирование промпта с контекстом
4. Генерация ответа через LLM
5. Сохранение результата в кеш

### 4. GigaChatClient / OpenAI Client

Отвечает за:
- Авторизацию в API
- Управление токенами доступа
- Отправку запросов к LLM
- Получение embeddings (для GigaChat)

## 📊 Оценка качества

Система поддерживает автоматическую оценку качества через библиотеку RAGAS. Доступны следующие метрики:

- **Faithfulness** (Точность ответа): Измеряет соответствие ответа предоставленному контексту (0.0 - 1.0)
- **Context Precision** (Точность контекста): Оценивает качество извлечённого контекста для ответа на вопрос (0.0 - 1.0)

Для запуска оценки:
```bash
cd assistant_api
python evaluate_ragas.py
```

## 🛠️ Технологии

- **Python 3.11+** — основной язык программирования
- **ChromaDB** — векторная база данных
- **OpenAI API** — для embeddings и генерации текста
- **GigaChat API** — альтернативный провайдер LLM от Сбера
- **SQLite** — для кеширования результатов
- **RAGAS** — библиотека для оценки качества RAG систем
- **sentence-transformers** — для работы с embeddings
- **langchain** — для интеграции с LLM

## 📝 Примеры использования

### Базовый запрос

```python
from rag_pipeline import RAGPipeline

pipeline = RAGPipeline()
result = pipeline.query("Что такое машинное обучение?")
print(result['answer'])
```

### Запрос без кеша

```python
result = pipeline.query("Что такое RAG?", use_cache=False)
```

### Получение статистики

```python
stats = pipeline.get_stats()
print(f"Документов в хранилище: {stats['vector_store']['count']}")
print(f"Записей в кеше: {stats['cache']['total_entries']}")
```

### Очистка кеша

```python
pipeline.cache.clear()
```

## 🔒 Безопасность

⚠️ **Важно:**
- Никогда не публикуйте файл `.env` с реальными API ключами
- Добавьте `.env` в `.gitignore`
- Храните API ключи в безопасном месте
- Не передавайте ключи через незащищённые каналы связи

## 🤝 Вклад в проект

Приветствуются любые улучшения! Пожалуйста:
1. Создайте форк проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в образовательных целях. Используйте на своё усмотрение.

## 🙏 Благодарности

- Команде ChromaDB за отличную векторную базу данных
- OpenAI за предоставление API для работы с LLM
- Сберу за GigaChat API
- Разработчикам RAGAS за инструменты оценки качества

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

**Приятного использования! 🚀**
